{
  "name": "kot-vreditel",
  "private": true,
  "version": "1.0.0",
  "description": "Аркада про кота, который сбрасывает вещи со стола, пока хозяин не смотрит",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:pages": "tsc && GITHUB_PAGES=true vite build",
    "deploy:gh-pages": "npm run build:pages && npx gh-pages@6.2.0 -d dist --dotfiles -m \"Deploy planning app\"",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.3.0"
  }
}
